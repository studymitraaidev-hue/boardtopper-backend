name: Keep Render Awake

on:
  schedule:
    - cron: '*/10 * * * *'   # Every 10 minutes
  workflow_dispatch:

jobs:
  ping-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Ping BoardTopper Backend
        run: |
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}"             https://boardtopper-backend.onrender.com/api/health             || echo "000")
          echo "Health check returned: $HTTP_CODE"
          if [ "$HTTP_CODE" != "200" ]; then
            echo "Backend may be down or sleeping"
            exit 0
          fi
          echo "Backend is awake and responding"
